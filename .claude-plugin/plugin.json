{
  "name": "velo",
  "version": "2.1.0",
  "description": "Velo V2 — two modes: Ask (off-axis, read-only conceptual Q&A that never touches the repo) and Plan (a persisted, versioned plan saved under .velo/ with an explicit approval freeze — planning only, never execution). Neither mode starts Run or Auto.",
  "author": {
    "name": "Raja"
  }
}
